// Vorlage für lokale, NICHT versionierte Überschreibungen.
//
// Einmalig einrichten:
//   cp config-override.example.typ config-override.typ
//
// Danach hier nur die Felder eintragen, die von den Standardwerten in
// `config.typ` abweichen sollen. Alles andere bleibt auf dem Standard.
#let overrides = (
  // vorname: "Erika",
  // name: "Mustermann",
  // thema: "Titel meiner Abschlussarbeit",
  // professor: "Prof. Dr. Beispiel",
  // datum: "01.01.2026",
)
